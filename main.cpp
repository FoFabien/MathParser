#include "cmd.hpp"

// test strings
/*std::string str[] =
{
    "pi+1.1",
    "pi*2.2",
    "2.2*pi+1.1",
    "(2.2*pi+1.1)*3.3",
    "sin(2.2 * pi) + cos(pi / 2)",
    "1 - sin(2.2 * pi) + cos(pi / 2)",
    "sqrt(1 - sin(2.2 * pi) + cos(pi / 2) / 3.3)",
    "(pi^2 / sin(2 * pi / 2)) -3 / 2.2",
    "1-(4/pi*0.5)",
    "6.96^log(7*pi)",
    "10^log(3+pi)",
    "(cos(2.41)/pi)",
    "-(sin(pi+3)+1)",
    "5-(6.96^(log(7+pi)))"
};*/


int main()
{
    CMD test;
    test.run();
    return 0;
}
